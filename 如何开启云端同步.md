# Jill 工作台 · 云端同步配置指南

> 目标：让电脑和手机上的客户、饮食、情报数据保持一致。
> 预计耗时：5 分钟。不需要写代码，只需要复制粘贴。

---

## 一、注册免费的 Supabase 账号

1. 打开 [https://supabase.com](https://supabase.com)，点 **Start your project**。
2. 用邮箱/GitHub/Google 注册并登录。
3. 点击 **New project**，取个项目名（例如 `jill-workspace`）。
4. 设置数据库密码（自己记好），地区选 **Singapore** 或 **Tokyo**（离中国近）。
5. 点 **Create new project**，等待 1-2 分钟项目创建完成。

---

## 二、确认 Data API 已开启

创建项目时，确保勾选了 **Enable Data API**。如果当时没勾，现在去左侧菜单 **Integrations → Data API**，把开关打开。

## 三、创建数据表

用 SQL 编辑器一次性建表并授权（比图形界面快，也不容易漏权限）：

1. 左侧菜单进入 **SQL Editor**。
2. 新建一个查询，把下面整段复制进去，点右上角 **Run**（▶ 按钮）：

```sql
create table if not exists sync_bucket (
  id text primary key,
  payload jsonb,
  updated_at timestamptz default now()
);

-- 关闭行级安全策略（简化配置，用同步码做隔离）
alter table sync_bucket disable row level security;

-- 给匿名角色授权（Supabase 新版 publishable key 使用 anon 角色访问 Data API）
grant usage on schema public to anon;
grant select, insert, update, delete on public.sync_bucket to anon;
```

3. 执行成功后会显示 `Success. No rows returned.`。

> 如果你更喜欢图形界面：进入 **Table Editor → New table**，Name 填 `sync_bucket`，RLS 关闭，然后添加 `payload`（jsonb）和 `updated_at`（timestamptz，默认值 `now()`）两个字段。但**最后必须回到 SQL Editor 执行上面的 `grant ... to anon` 授权语句**，否则工作台会报 `Failed to fetch`。

---

## 四、拿到 API 信息

1. 左侧菜单进入 **Project Settings → General**，复制 **Project ID**，你的 **Project URL** 就是：
   ```
   https://[Project ID].supabase.co
   ```
   例如 Project ID 是 `goaucudilgknlmre`，URL 就是 `https://goaucudilgknlmre.supabase.co`。

2. 左侧菜单进入 **Project Settings → API Keys**。
3. 复制 **Publishable key** 下面 `default` 那一行（以 `sb_publishable_` 开头）。**不要复制 Secret keys**。

---

## 五、在工作台里填入配置

1. 打开 Jill 的工作台（电脑本地文件或网页链接都行）。
2. 点击右上角 **☁️ 云朵图标**（手机在底部导航栏也能找到）。
3. 填入：
   - **Supabase 项目 URL**：刚才拼好的 URL
   - **anon public API key**：刚才复制的 Publishable key
   - **同步码**：自己定一个口令，例如 `jill2026`，所有设备填同一个才能互通
4. 点 **连接并同步**。
5. 看到状态变成「已同步」就成功了。

---

## 六、在手机上使用

1. 手机浏览器打开部署好的网页链接（Mochi 会发给你）。
2. 点击浏览器菜单 → **添加到主屏幕** / **安装应用**。
3. 打开 App，用同样的同步码连接 Supabase。
4. 等 3-5 秒，电脑上的客户和饮食记录会自动出现在手机里。

---

## 七、数据同步规则

- **客户、饮食、设置**：任何一端修改后，大约 2-3 秒会自动上传到云端，另一端打开时自动下载。
- **行业情报**：电脑本地文件每天 9:30 自动更新后，打开工作台会自动把最新情报推送到云端；手机/网页端打开时会自动拉取云端最新情报。
- **冲突处理**：以时间戳为准，哪边最新就用哪边。

---

## 八、安全提示

- API key 只存在你自己的浏览器 localStorage 和工作台配置里，不会上传到任何其他地方。
- 不要把同步码和 API key 告诉他人。
- 建议每周点一次工作台侧边栏的「备份 / 恢复数据 → 导出」，留一个本地备份。

---

## 九、常见问题

**Q：状态显示「失败：Failed to fetch」怎么办？**
A：按顺序检查：
1. Supabase 左侧 **Integrations → Data API** 是否开启。
2. SQL Editor 里是否执行了完整的建表 + `grant ... to anon` 授权语句。
3. Project URL 是否拼对（`https://` + Project ID + `.supabase.co`）。
4. 填的是否是 **Publishable key**（`sb_publishable_` 开头），不是 Secret key。

**Q：状态显示「失败：401」怎么办？**
A：API key 填错了，或者 RLS 没关。请回到 Supabase Table Editor，确认 `sync_bucket` 表的 RLS 是关闭状态。

**Q：状态显示「失败：403 / permission denied」怎么办？**
A：表建好了但 anon 角色没权限。回到 SQL Editor 执行：
```sql
grant usage on schema public to anon;
grant select, insert, update, delete on public.sync_bucket to anon;
```

**Q：为什么电脑改了，手机没立刻变？**
A：手机端需要重新打开 App 或下拉刷新。也可以点云朵图标 → **从云端下载**。

**Q：同步码可以改吗？**
A：可以，但所有设备必须用同一个同步码。换同步码等于换了一个新的「数据空间」。
